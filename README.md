# VMAP

A vulnerability-exploit desktop finder.

Vmap use the [vulners.com](https://vulners.com) API.

## INSTALLATION

```shell
qt      >= 5.x
openssl >= 1.1
```
```shell
qmake
make
make install
```
